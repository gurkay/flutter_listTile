import 'package:flutter/material.dart';

class ItemData2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('test item data 2'),
      ),
      body: Center(
        child: Text('data 2'),
      ),
    );
  }
}
